interface:
  display_name: "Megapack"
  short_description: "VCont Megapack CI and .run validation"
  default_prompt: "Use $megapack to validate a VCont Megapack .run artifact, inspect release-build behavior, or review installer flags and package matrix."
