interface:
  display_name: "Megapack"
  short_description: "VCont Megapack .run validation"
  default_prompt: "Use $megapack to validate a VCont Megapack .run artifact or inspect the local Megapack repo."
