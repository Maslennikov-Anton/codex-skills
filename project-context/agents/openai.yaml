interface:
  display_name: "Project Context"
  short_description: "Local repo map and recurring commands"
  default_prompt: "Use $project-context to orient in a non-trivial /home/ant/IdeaProjects task: repo purpose, trusted commands, generated files and dirty-worktree risks."

policy:
  allow_implicit_invocation: true
