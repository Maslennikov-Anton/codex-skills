interface:
  display_name: "Project Context"
  short_description: "Local repo map and recurring commands"
  default_prompt: "Use $project-context to orient on this local repository before changing code or running project commands."

policy:
  allow_implicit_invocation: true
