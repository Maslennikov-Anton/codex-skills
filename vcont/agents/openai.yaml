interface:
  display_name: "VCont"
  short_description: "Runtime, Modbus, Profibus, HSB и bootfile VCont"
  default_prompt: "Используй vcont skill, чтобы разобрать конфиг, bootfile, Modbus/Profibus, HSB или поведение runtime VCont."

policy:
  allow_implicit_invocation: true
